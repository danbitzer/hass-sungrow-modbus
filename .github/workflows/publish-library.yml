name: Publish library to PyPI

# Tag the library release as lib-vX.Y.Z; the tag must equal the version in
# packages/sungrow-sht-modbus/pyproject.toml (moved with scripts/sync_version.py).
on:
  push:
    tags: ["lib-v*"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - name: Check the tag matches the library version
        run: |
          tag="${GITHUB_REF_NAME#lib-v}"
          version="$(sed -n 's/^version = "\(.*\)"$/\1/p' packages/sungrow-sht-modbus/pyproject.toml)"
          test "$tag" = "$version" || { echo "tag $tag != version $version"; exit 1; }
      # A workspace member builds into the workspace root's dist/.
      - run: uv build --package sungrow-sht-modbus --out-dir dist
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          if-no-files-found: error

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
